<%= form_for @recipe do |f| %>
  Title: <%= f.text_field :title %>

  <%= f.fields_for :ingredients do |ingredient| %>
    name: <%= ingredient.text_field :name %><br>
    quantity: <%= ingredient.text_field :quantity %>
  <% end %>

  <%= f.submit %>
<% end %>
