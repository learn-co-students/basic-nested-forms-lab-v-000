<%= form_for @recipe do |f| %>
  Title: <%= f.text_field :title %>
  Ingredients:

  <%= f.fields_for :ingredients do |ingredient| %>
  	<%= ingredient.label :name %>
  	<%= ingredient.text_field :name %> <br>

  	<%= ingredient.label :qty %>
  	<%= ingredient.number_field :quantity%>
  	<% end %>

  	<%= f.submit %>
<% end %>
