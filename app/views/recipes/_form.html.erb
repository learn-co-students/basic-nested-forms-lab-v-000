<%= form_for @recipe do |f| %>
  Title: <%= f.text_field :title %>
  Ingredients: <%= f.fields_for :ingredients do |ingredient| %>
    Name: <%= ingredient.text_field :name %>
    Quantity: <%= ingredient.text_field :quantity %>
  <% end %>
  <%= f.submit %>
<% end %>
