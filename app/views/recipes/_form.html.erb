<%= form_for @recipe do |f| %>
  Title: <%= f.text_field :title %><br><br>

  <%= f.fields_for :ingredients do |item| %>
    Name: <%= item.text_field :name %><br>
    Quantity: <%= item.text_field :quantity %><br><br>
  <% end %>

  <%= f.submit %>
<% end %>
