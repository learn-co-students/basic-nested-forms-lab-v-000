<h1> New Recipe </h1>
<%= form_for @recipe do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %><br>

  <%= f.fields_for :ingredients do |ingredients| %>
    <%= ingredients.label :name %>
    <%= ingredients.text_field :name %><br>

    <%= ingredients.label :quantity %>
    <%= ingredients.text_field :quantity %>
    <%end%>
  <%= f.submit %>
<% end %>
